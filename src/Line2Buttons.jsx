import Button from './Button'

export default function Line2Buttons() {
  return (
    <div>
        <Button label="7" onClick={() => {}} />
        <Button label="8" onClick={() => {}} />
        <Button label="9" onClick={() => {}} />
        <Button label="*" onClick={() => {}} />
    </div>
  )
}