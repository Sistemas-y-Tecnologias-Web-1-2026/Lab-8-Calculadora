import Button from './Button'

export default function Line1Buttons() {
  return (
    <div>
        <Button label="C" onClick={() => {}} />
        <Button label="+/-" onClick={() => {}} />
        <Button label="%" onClick={() => {}} />
        <Button label="/" onClick={() => {}} />
    </div>
  )
}