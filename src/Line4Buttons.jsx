import Button from "./Button";

export default function Line4Buttons() {
  return (
    <div>
        <Button label="1" onClick={() => {}} />
        <Button label="2" onClick={() => {}} />
        <Button label="3" onClick={() => {}} />
        <Button label="+" onClick={() => {}} />
    </div>
  )
}