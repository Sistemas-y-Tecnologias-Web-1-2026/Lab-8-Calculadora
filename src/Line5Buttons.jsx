import Button from './Button'

export default function Line5Buttons() {
  return (
    <div>
        <Button label="0" onClick={() => {}} />
        <Button label="." onClick={() => {}} />
        <Button label="=" onClick={() => {}} />
    </div>
  )
}   